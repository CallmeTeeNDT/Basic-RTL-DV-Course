## SESSION 02 – LINUX, VIM, EDA

This session introduced the fundamental development environment commonly used in RTL Design and Design Verification. I practiced navigating and managing files in Linux, editing source files with Vim, organizing a basic RTL project structure, and preparing the required file lists and simulation environment. The session also provided my first hands-on experience with the workflow of managing RTL source code, testbench files, simulation scripts, and EDA tools from the command line.

### My Exercises

- **Homework 1 – Vim Practice**
  - Completed all lessons in the provided `vimtutor.txt`
  - Practiced essential Vim navigation and editing commands
  - Marked each completed lesson and verified completion using Linux commands
  - Used `grep` and `wc` to confirm all required exercises were completed
- **Homework 2 – Makefile Automation**
  - Investigated the structure and usage of a Makefile
  - Added a `make help` target to display available commands
  - Added a `make clean` target to remove simulation-generated files
  - Added a `make all` target to clean, build, and run the simulation automatically
  - Integrated the updated Makefile into the existing simulation environment

### What I Practiced

- Navigating and editing files efficiently with Vim
- Working with Vim command and insert modes
- Using common Vim operations for navigation, editing, searching, saving, and exiting
- Applying Linux text-processing commands such as `grep` and `wc`
- Understanding the role of a Makefile in an RTL simulation environment
- Defining and organizing Makefile targets
- Automating repetitive simulation commands
- Cleaning generated simulation files before rebuilding
- Combining clean, build, and run steps into a single workflow
- Developing familiarity with a Linux-based RTL/DV development environment
